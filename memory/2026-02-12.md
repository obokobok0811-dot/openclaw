2026-02-12 session notes

- User: Andy (telegram id 5510621427). Timezone: Asia/Seoul.
- Goal: Connect Telegram bots (shin, obok) and manage multi-bot behavior; produce Singapore 2/13-2/18 family itinerary and PDFs; implement replyer dedupe and other automation.

Actions taken (durable):
- Telegram credentials and pairing files created/managed under credentials/:
  - credentials/telegram_bot.json (shin) — active
  - credentials/telegram_bot_Openclaw_obok_bot.json was stored then removed per user request on 2026-02-12 ~19:38 KST.
  - credentials/telegram_pairing.json updated (bots list cleared after obok removal).
- Forwarder/replyer architecture:
  - forwarder_to_main.py, replyer.py implemented/modified.
  - Global deduplication (LRU) added to replyer (recent_global deque + recent_set) to skip recent duplicate (bot,chat_id,text) entries.
  - replyer and related processes restarted after patching. replyer PID ~4552/4680 depending on restarts.
- Auto-responders:
  - auto_responder.py (per-bot) modified to remove prefixed phrases like "답변:" and "메시지를 잘 받았습니다"; now concise plain replies.
  - obok-specific behavior adjusted: concise/no-prefix answers; later obok bot was unpaired and token file removed per user's request.
  - Shin auto_responder restarted and running (credentials/telegram_bot.json).
- Message-length policy:
  - Increased preserved input length to 4000 characters (Telegram safe margin). Both replyer and auto_responder adjusted accordingly.
- Files/PDFs created and sent to Telegram (Andy):
  - workspace/singapore_4n5d_summary_korean.pdf (created earlier)
  - workspace/itinerary_2026-02-13_to_02-18_korean.pdf (initial)
  - workspace/itinerary_final_2026-02-13_to_02-18_korean.pdf (final, includes child-friendly adjustments)
  - All PDFs sent to Telegram (message IDs recorded in session logs).
- Maps/route images:
  - Generated per-day taxi-route schematic images workspace/maps/*_route.png and sent to Telegram.

User preferences & constraints (durable):
- Language: Korean preferred for interactive messages.
- Telegram behavior:
  - Do NOT forward terminal/chat content to Telegram unless the message is an explicit reply to a recent Telegram-origin message.
  - Do not include the specific phrases: leading prefix "답변:" and initial phrase "메시지를 잘 받았습니다" in bot replies.
  - Shin & obok: concise replies; obok should not ask follow-ups and must reply concisely when active.
  - Allowed message window and reply behavior: adjusted; processes restarted to reflect changes.
- Travel preferences: family-friendly, prioritize child comfort (morning highlights, minimize evening shows if child tired), Hotel MI ROCHOR from 2/14.

Outstanding / next steps (user may request):
- Gateway config to enable brave web_search remains unresolved (config.schema issues).
- LLM-based replyer awaits LLM_API credentials if the user wants richer replies.
- Recommend rotating any bot tokens that appeared in chat; user removed obok token file.
- Optional: generate Google Maps driving links per segment, KML/GeoJSON export, or higher-fidelity map snapshots (may need API keys).

Recorded: 2026-02-12T10:40:00+09:00 by OpenClaw agent
