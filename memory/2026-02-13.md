2026-02-13 Durable memory snapshot

Context & actions taken:
- Telegram user: Andy (user_id 5510621427), timezone Asia/Seoul, prefers Korean.
- Paired bot: Openclaw_zzuvi_bot (id 7955259296). Stored credential at credentials/telegram_bot.json (user-provided token). Pairing metadata updated in credentials/telegram_pairing.json.

Morning updates (09:16 KST)
- Issue: User reported external connections failing (mobile/outside Wi‑Fi unable to reach local services).
- Diagnosis: Many local gateway/bridge processes were not running (ps showed no python/node services). Logs showed replyer.log had recent history but many other logs were empty.
- Actions taken by assistant:
  - Restarted core services as requested (backgrounded):
    - forwarder_to_main.py — PID 2044
    - replyer.py — PID 2046
    - auto_responder.py — PID 2049
    - tools/telegram_command_queue.py — PID 2051
    - tools/command_approver.py — PID 2054
    - tools/telegram_to_fifo.py — PID 2056
    - tools/terminal_to_telegram.py — PID 2059
  - Verified replyer.log shows normal activity (duplicate-skip logs and successful sends).
  - Confirmed workspace FIFOs and files exist: workspace/telegram_fifo, workspace/command_approver_fifo, pending_commands.jsonl, forwarded_messages.jsonl.

Security & preferences reaffirmed:
- APPROVE FIFO is sensitive — do not expose externally. Default policy: do not publish or open FIFOs to public internet. Use VPN/SSH for remote approvals.
- User prefers Korean language and limited forwarding; flows were adjusted per user earlier (no automatic log forwarding; user stopped log_tailer_to_telegram). Assistant must request explicit permission before restarting log forwarding.

Outstanding items / next steps suggested (not yet executed):
- Configure process supervision (systemd/pm2/launchd) so services auto-restart after reboot. (User permission required to install service units.)
- Optionally create a Cloudflare Tunnel or ngrok instance for stable external webhook access; user to choose. (Not executed.)
- Harden FIFOs (chmod 600) and restrict access; await user permission.

Files/tickets touched during morning restart
- Updated logs: logs/replyer.log (appended), logs/forwarder_shin.log, logs/auto_responder_shin.log, logs/telegram_command_queue.log, logs/command_approver.log, logs/telegram_to_fifo_runner.log, logs/terminal_to_telegram_runner.log (new PIDs logged).
- Workspace unchanged except process state and logs; existing memory file updated.

Saved: 2026-02-13 09:17 KST
